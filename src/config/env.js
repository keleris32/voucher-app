import {
  BACKEND_URL,
  EMAIL_CALLBACK_URL,
  VERIFICATION_CALLBACK_URL,
} from '@env';

const EnvironmentVariables = {
  BACKEND_URL,
  EMAIL_CALLBACK_URL,
  VERIFICATION_CALLBACK_URL,
};

export default EnvironmentVariables;
